cmake_minimum_required(VERSION 3.17)
project(CubicOctree)
set(CMAKE_CXX_STANDARD 20)

add_subdirectory(src)
option(TRO_TEST "Enable test executable" TRUE)
if (${TRO_TEST})
    option(FUZZ_TEST "Test using libFuzz" FALSE)
    add_subdirectory(test)
endif()